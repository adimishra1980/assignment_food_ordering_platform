# frontend/nginx.conf
server {
  listen 80;

  location / {
    root   /usr/share/nginx/html;
    index  index.html;
    # This line is crucial for React Router. It redirects all 404s to index.html.
    try_files $uri $uri/ /index.html;
  }
}